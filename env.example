# Silvia PID Environment Configuration
# Copy this file to .env and adjust values as needed

# ===========================
# SSL Configuration
# ===========================
# Set to 'true' to enable HTTPS, 'false' for HTTP only
USE_SSL=false

# SSL certificate paths (only needed if USE_SSL=true)
# These should point to your Let's Encrypt or other SSL certificates
SSL_KEY_PATH=/etc/letsencrypt/live/your-domain.com/privkey.pem
SSL_CERT_PATH=/etc/letsencrypt/live/your-domain.com/fullchain.pem

# ===========================
# Port Configuration
# ===========================
# HTTP port (default: 80)
HTTP_PORT=80

# HTTPS port (default: 443, only used if USE_SSL=true)
HTTPS_PORT=443

# ===========================
# MongoDB Configuration
# ===========================
# MongoDB connection URL
# For Docker: mongodb://mongodb:27017
# For local: mongodb://localhost:27017
MONGODB_URL=mongodb://mongodb:27017

# ===========================
# Hardware Configuration
# ===========================
# These are typically not needed as environment variables
# but can be set if you need custom paths

# Path to config.json file
# CONFIG_FILE=/app/config.json

# ===========================
# Development vs Production
# ===========================
# Uncomment for local development without Docker:
# MONGODB_URL=mongodb://localhost:27017
# USE_SSL=false
# HTTP_PORT=3000

# ===========================
# Notes
# ===========================
# 1. SSL requires ports 80 and 443, which need root or capabilities
# 2. For development, use HTTP_PORT=3000 or similar to avoid needing root
# 3. GPIO and I2C devices (/dev/gpiomem, /dev/i2c-1) must be accessible
# 4. MongoDB must be running and accessible at MONGODB_URL

